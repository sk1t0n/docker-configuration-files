services:
  uptime-kuma:
    image: louislam/uptime-kuma:alpine
    container_name: uptime-kuma
    restart: on-failure
    ports:
      - '3001:3001'
    volumes:
      - uptime-kuma-data:/app/data

volumes:
  uptime-kuma-data:
